version: '3.8'

services:
  asterisk:
    image: andrius/asterisk
    container_name: asterisk
    restart: always
    ports:
      - "5060:5060/udp"   # SIP
      - "5060:5060/tcp"
      - "10000-10010:10000-10010/udp" # RTP (áudio)
    volumes:
      - ./config:/etc/asterisk
